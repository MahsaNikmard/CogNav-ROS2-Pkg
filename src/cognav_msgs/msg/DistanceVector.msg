# Padded polar distance vector published by cognav_perception, one range per
# bin in metres, leftmost bin first. header.stamp is the source image stamp.
std_msgs/Header header

float32[] distance_vector
